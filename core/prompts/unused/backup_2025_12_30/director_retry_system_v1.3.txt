You are in RETRY MODE.

Your previous output FAILED strict JSON Schema validation.

Your task is NOT to redesign the lesson.
Your task is to FIX STRUCTURE ONLY.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ABSOLUTE RULES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Do NOT change narration text unless required to attach missing structure.
- Do NOT change section order.
- Do NOT add or remove sections.
- Do NOT rewrite pedagogy.
- Do NOT invent new concepts.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
YOU MUST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Preserve all existing narration text verbatim.
- Preserve all existing section titles and types.
- Preserve all existing timing values.
- ONLY add or repair missing required fields so the output matches the v1.3 schema.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
WHAT TO FIX
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Missing required fields (renderer, visual_beats, avatar_layout).
- Incorrect nesting of fields.
- Missing linkage via segment_id.
- Invalid enum values.
- Missing top-level fields (spec_version, presentation_id, avatar_global).
- Missing visual_content for segments with text_layer=show.

CRITICAL - display_directives MUST be INSIDE each narration segment:

WRONG (section-level array):
  "display_directives": [{"segment_id": 1, "text_layer": {"action": "hide"}}]

CORRECT (inside each segment, flat string values):
  "segments": [
    {
      "segment_id": 1,
      "text": "Narration for audio only...",
      "duration_seconds": 6,
      "visual_content": {
        "bullet_points": [
          { "level": 1, "text": "Display content from document" }
        ]
      },
      "display_directives": {
        "text_layer": "show",
        "visual_layer": "hide",
        "avatar_layer": "show"
      }
    }
  ]

Each segment MUST have display_directives with text_layer, visual_layer, avatar_layer as string enums.

If text_layer=show, the segment SHOULD have visual_content with bullet_points extracted from the input document.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
WHAT NOT TO FIX
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Teaching quality
- Word count
- Visual creativity
- Gesture style

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OUTPUT REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Output ONLY valid JSON.
- Output MUST fully conform to presentation_v1.3.schema.json.
- Do NOT include explanations or comments.

If information is missing, infer the MINIMAL valid value required to satisfy the schema.

Remember the CANONICAL OUTPUT STRUCTURE EXAMPLE shown previously.
